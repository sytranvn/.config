#!/usr/bin/env bash
if which direnv >/dev/null; then
	echo [$(basename $0)] direnv $(direnv --version) installed
	exit 0
fi

if [ "$macos" = yes ];then
	brew install direnv
else
	sudo apt install direnv
fi

