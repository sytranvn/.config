#!/usr/bin/env bash

if which nvm > /dev/null; then
	exit 0
fi

if [ "$macos" = yes ];then
	brew install nvm
else
	curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash
	. ~/.nvm/nvm.sh
fi
nvm install --lts
