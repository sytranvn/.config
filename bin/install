#!/usr/bin/env bash

bindir=$(dirname $0)
export PATH="$bindir:$PATH"
if [ "$(uname -s)" = Darwin ]; then
	macos=yes
else
	sudo apt update
fi
export macos

install-kitty

install-direnv

install-neovim

install-bash

install-node
